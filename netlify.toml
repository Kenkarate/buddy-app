[build]
  command = "next build"
  publish = ".next"

# Runs the unified Next.js app (SSR pages, /api route handlers, the Edge proxy
# and the Razorpay webhook) on Netlify. Replaces the old SPA + single
# serverless-http function setup.
[[plugins]]
  package = "@netlify/plugin-nextjs"
